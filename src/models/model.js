const mongoose = require("mongoose");
const Schema = mongoose.Schema;


// export to the Controller Page
module.exports = {
  User,
  Admin,
  Renter,
  VehicalOwner,
  VehicalDetails,
  VehicleImage,
  BookingDetails,
  Feedback,
  Payment,
  Contact,
};
